- Como realizar un README.md para los enpoint.  --> https://docs.github.com/es/rest/repos/contents?apiVersion=2022-11-28
- 7 tipos de datos primitivos de JS. --> string, number, bigint, boolean, undefined, symbol y null,
- Cuales son las maneras de realizar input y output en JS.  --> input (prompt y form), output ("console.log" y manipulacion del dom "innerHTML")
- Que es el DOM en JS --> Las siglas DOM significan Document Object Model, o lo que es lo mismo, la estructura del documento HTML. Una página HTML está formada por múltiples etiquetas HTML, anidadas una dentro de otra, formando un árbol de etiquetas relacionadas entre sí, que se denomina árbol DOM (o simplemente DOM).